{
  "name": "prompt-improver",
  "version": "0.1.0",
  "description": "AI-powered prompt improvement extension for LLM platforms",
  "type": "module",
  "scripts": {
    "dev": "node build.js --watch --dev",
    "build": "node build.js",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:verbose": "jest --verbose"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@types/chrome": "^0.0.268",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.5",
    "esbuild": "^0.19.12",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.3.3"
  }
}